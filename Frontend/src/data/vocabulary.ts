export interface Vocabulary {
    id: number;
    term: string;
    definition: string;
    example?: string;
    createdAt: string;
    // imageUrl?: string;
    // audioUrl?: string;
    // updatedAt: Date;
}